#include <iostream>
#include <windows.h>
#include "EDRAgent.h"

int main() {
    std::cout << "AKIR EDR Agent Starting..." << std::endl;
    
    // TODO: Connect to Kernel Driver
    // TODO: Start Event Loop
    
    while (true) {
        Sleep(1000);
    }
    
    return 0;
}
